# Second Brain — kernel dependencies
#
# The kernel aims to stay close to pure Python. Capabilities beyond the core
# (search, embeddings, OCR, audio, integrations, Telegram, …) are installed from
# the store, and each store plugin declares its own extra dependencies.

# --- File watcher (pipeline/watcher) ---
watchdog

# --- Timekeeper (services/timekeeper) ---
cron-descriptor
croniter

# --- Pytest ---
#
# xdist is not optional tooling: `pytest.ini` runs `-n auto` by default,
# because the suite is ~2200 tests whose cost is real subprocess boxes and
# deliberate timeout waits. Serial it is 74s, parallel it is 16s. Pass `-n0`
# to turn it off for a debugging run.
pytest
pytest-xdist
